#ifndef DEFINES_H
#define DEFINES_H

#define repeat(i, t) for(int i = 0; i < (int)t; ++i)
#define repeat_u(i, t) for(unsigned i = 0; i < t; ++i)

#define I_TO_X(i, size) (i%size)
#define I_TO_Y(i, size) (i/size)

#endif // DEFINES_H

